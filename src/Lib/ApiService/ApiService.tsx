
export const stateData = async (dispatch: any) => {
    try {
        // const response: any = await Get_Api(ApiUrl.state_list)();
        // dispatch(setState(response?.data || []));
    } catch (error) {
    }
};




